# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
# Standalone sage_lint binary copied into the Sublime package (built per-OS, not committed).
sage_lint/plugins/sublime/bin/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

data
Replays
.claude/settings.local.json
parser.sublime-project
parser.sublime-workspace
test.py
thoughts.txt
*.txt
parse_enumerators.py
parse_behaviors.py
current_issues.txt
*.ini
ini-parser.sublime-project
ini-parser.sublime-workspace
pages
OpenSAGE-master

# Test fixtures are real ini/txt files that must be tracked despite the global
# `*.ini`/`*.txt` ignores above (the `data/` corpus stays ignored).
!tests/fixtures/
!tests/fixtures/**
object_ids.*.json
docs/apt_guidelines.pdf

sage_patch/patches/template_checksum.py

# Per-package planning / working notes are local scratch, not committed.
_local/
PLAN.md
TODO.md
HANDOVER.md
corpus_wishlist.md
ToFix.md
gpl_xfer_reference.md
sav_format.md
aggregate/
sage_replay/format_coverage_plan.md
sage_replay/order_space_map.md
intro.md
pySAGE.zip
# Copyrighted game binaries and any patched output (`sage-patch apply` writes these): the tool
# ships the patch recipe, never the binary. Matched by basename so they are ignored wherever they
# land, not only under sage_patch/engine/.
game.dat
game.dat.backup
patched.dat
Worldbuilder.exe
sage_patch/game.dat.asm
